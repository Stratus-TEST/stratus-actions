name: Simple Release

on:
  pull_request:
    types: [closed]
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  packages: write

jobs:
  create-release:
    if: github.event.pull_request.merged == true || github.event_name == 'workflow_dispatch' && github.repository == 'stratus-test/stratus-gh-actions' # Only run on the base repository
    runs-on: ubuntu-latest
    outputs:
      docker_dirs: ${{ steps.find-docker.outputs.dirs }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Create Release
        id: release
        uses: ./release
        with:
          draft: false
          prerelease: false

  docker-build:
    needs: create-release
    if: github.event.pull_request.merged == true || github.event_name == 'workflow_dispatch' && github.repository == 'stratus-test/stratus-gh-actions' # Only run on the base repository
    uses: stratus-test/stratus-gh-actions/github/workflows/docker-build.yml@${{ needs.create-release.outputs.release.tag_name }}
    with:
      ref: ${{ needs.create-release.outputs.release.tag_name }}